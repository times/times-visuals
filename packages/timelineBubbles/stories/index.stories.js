import React from 'react';
import { storiesOf } from '@storybook/react';

import TimelineBubbles from '../src';

storiesOf('Charts/TimelineBubbles', module).add('default', () => (
  <TimelineBubbles />
));
