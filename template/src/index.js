import React from "react";

export default () => (
  <main>
    This is just a template, to help you create new components. Copy the{" "}
    <code>./packages/template</code> directory to{" "}
    <code>./packages/(your-package-name-here)</code> and update the{" "}
    <code>package.json</code> and <code>README.md</code> with your package's
    name and description.
  </main>
);
